import DashboardLayout from "../(dashboard)/layout";

export default function ContactsLayoutWrapper({ children }: { children: React.ReactNode }) {
  return <DashboardLayout>{children}</DashboardLayout>;
}
